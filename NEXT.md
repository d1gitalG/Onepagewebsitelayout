# NEXT — Omnitrix Website (Deploy + Contact Form + Lead Gen)

## Now (ASAP)
- **Deploy the site**
  - Pick host: Vercel / Netlify (fast).  
  - DoD: `omnitrix.tech` loads the latest build, HTTPS, no console errors.

- **Contact form backend (no custom server)**
  - Recommendation: **Web3Forms** (simple) unless you already have a provider preference.
  - DoD: form submits → you receive an email at **info@omnitrix.tech** + user sees success state.

- **Lead-gen pipeline starter**
  - DoD: one repeatable workflow doc + a first batch of outreach targets ready to send.
  - Uses existing: `Omnitrix_Intro_Letter.md` + `leads_list_GC_MSP.md`.

## Next
- Add basic analytics (Plausible/GA4) + conversion event on form submit.
- Add 1–2 trust blocks: licenses, service area, past work photos (even small).

## Later
- SEO pass (title/meta/OG, local keywords, Google Business Profile tie-in).
- Landing pages per service (structured for GC/MSP subcontracting).
